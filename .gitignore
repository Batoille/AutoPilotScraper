# — Ignore Maven/Gradle build outputs —
target/
build/

# — Ignore local configuration file —
config.properties

# — Ignore IDE project files —
# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr

# Eclipse
.classpath
.project
.settings/

# VS Code
.vscode/

# — Ignore OS-specific junk —
.DS_Store
Thumbs.db

# — Ignore runtime database files (SQLite) —
*.db

# — Ignore Python cache (if you have Python scripts) —
__pycache__/
*.py[cod]

# — Ignore log or output folders/files —
logs/
*.log
records/
*.csv

# — Do NOT ignore these (so your bundled executables remain) —
!drivers/chrome_win64/chrome.exe
!drivers/chromedriver.exe
